///
/// LivelineMode.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/NitroHash.hpp>)
#include <NitroModules/NitroHash.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/JSIConverter.hpp>)
#include <NitroModules/JSIConverter.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif
#if __has_include(<NitroModules/NitroDefines.hpp>)
#include <NitroModules/NitroDefines.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

namespace margelo::nitro::liveline {

  /**
   * An enum which can be represented as a JavaScript union (LivelineMode).
   */
  enum class LivelineMode {
    LINE      SWIFT_NAME(line) = 0,
    CANDLE      SWIFT_NAME(candle) = 1,
  } CLOSED_ENUM;

} // namespace margelo::nitro::liveline

namespace margelo::nitro {

  // C++ LivelineMode <> JS LivelineMode (union)
  template <>
  struct JSIConverter<margelo::nitro::liveline::LivelineMode> final {
    static inline margelo::nitro::liveline::LivelineMode fromJSI(jsi::Runtime& runtime, const jsi::Value& arg) {
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, arg);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("line"): return margelo::nitro::liveline::LivelineMode::LINE;
        case hashString("candle"): return margelo::nitro::liveline::LivelineMode::CANDLE;
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert \"" + unionValue + "\" to enum LivelineMode - invalid value!");
      }
    }
    static inline jsi::Value toJSI(jsi::Runtime& runtime, margelo::nitro::liveline::LivelineMode arg) {
      switch (arg) {
        case margelo::nitro::liveline::LivelineMode::LINE: return JSIConverter<std::string>::toJSI(runtime, "line");
        case margelo::nitro::liveline::LivelineMode::CANDLE: return JSIConverter<std::string>::toJSI(runtime, "candle");
        default: [[unlikely]]
          throw std::invalid_argument("Cannot convert LivelineMode to JS - invalid value: "
                                    + std::to_string(static_cast<int>(arg)) + "!");
      }
    }
    static inline bool canConvert(jsi::Runtime& runtime, const jsi::Value& value) {
      if (!value.isString()) {
        return false;
      }
      std::string unionValue = JSIConverter<std::string>::fromJSI(runtime, value);
      switch (hashString(unionValue.c_str(), unionValue.size())) {
        case hashString("line"):
        case hashString("candle"):
          return true;
        default:
          return false;
      }
    }
  };

} // namespace margelo::nitro
