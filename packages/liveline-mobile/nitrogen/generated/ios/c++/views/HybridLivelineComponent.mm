///
/// HybridLivelineComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridLivelineComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridLivelineSpecSwift.hpp"
#import "LivelineMobile-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::liveline;
using namespace margelo::nitro::liveline::views;

/**
 * Represents the React Native View holder for the Nitro "Liveline" HybridView.
 */
@interface HybridLivelineComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridLivelineComponent {
  std::shared_ptr<HybridLivelineSpecSwift> _hybridView;
  BOOL _didDropView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridLivelineComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridLivelineComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    _props = HybridLivelineShadowNode::defaultSharedProps();
    std::shared_ptr<HybridLivelineSpec> hybridView = LivelineMobile::LivelineMobileAutolinking::createLiveline();
    _hybridView = std::dynamic_pointer_cast<HybridLivelineSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  LivelineMobile::HybridLivelineSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) notifyOnDropView {
  // A recycled component can later be invalidated. Notify only once per mount.
  if (_didDropView) {
    return;
  }
  LivelineMobile::HybridLivelineSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  _didDropView = YES;
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // A props update marks a newly mounted or still-active component.
  _didDropView = NO;

  // 1. Downcast props
  const auto& newViewProps = *std::static_pointer_cast<const HybridLivelineProps>(props);
  const auto* oldViewProps = static_cast<const HybridLivelineProps*>(oldProps.get());
  LivelineMobile::HybridLivelineSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update only props that differ from the previous Props snapshot.
  const bool hasTransactionPropChanges = oldViewProps == nullptr
      ? newViewProps.hasAnyProvidedProps()
      : !newViewProps.hasSameProps(*oldViewProps);
  if (hasTransactionPropChanges) {
    swiftPart.beforeUpdate();

    // data: optional
    if (oldViewProps == nullptr
          ? newViewProps.data.isProvided()
          : !newViewProps.data.hasSameValue(oldViewProps->data)) {
      swiftPart.setData(newViewProps.data.get());
    }
    // series: optional
    if (oldViewProps == nullptr
          ? newViewProps.series.isProvided()
          : !newViewProps.series.hasSameValue(oldViewProps->series)) {
      swiftPart.setSeries(newViewProps.series.get());
    }
    // value: optional
    if (oldViewProps == nullptr
          ? newViewProps.value.isProvided()
          : !newViewProps.value.hasSameValue(oldViewProps->value)) {
      swiftPart.setValue(newViewProps.value.get());
    }
    // mode: optional
    if (oldViewProps == nullptr
          ? newViewProps.mode.isProvided()
          : !newViewProps.mode.hasSameValue(oldViewProps->mode)) {
      swiftPart.setMode(newViewProps.mode.get());
    }
    // candles: optional
    if (oldViewProps == nullptr
          ? newViewProps.candles.isProvided()
          : !newViewProps.candles.hasSameValue(oldViewProps->candles)) {
      swiftPart.setCandles(newViewProps.candles.get());
    }
    // candleWidth: optional
    if (oldViewProps == nullptr
          ? newViewProps.candleWidth.isProvided()
          : !newViewProps.candleWidth.hasSameValue(oldViewProps->candleWidth)) {
      swiftPart.setCandleWidth(newViewProps.candleWidth.get());
    }
    // liveCandle: optional
    if (oldViewProps == nullptr
          ? newViewProps.liveCandle.isProvided()
          : !newViewProps.liveCandle.hasSameValue(oldViewProps->liveCandle)) {
      swiftPart.setLiveCandle(newViewProps.liveCandle.get());
    }
    // color: optional
    if (oldViewProps == nullptr
          ? newViewProps.color.isProvided()
          : !newViewProps.color.hasSameValue(oldViewProps->color)) {
      swiftPart.setColor(newViewProps.color.get());
    }
    // theme: optional
    if (oldViewProps == nullptr
          ? newViewProps.theme.isProvided()
          : !newViewProps.theme.hasSameValue(oldViewProps->theme)) {
      swiftPart.setTheme(newViewProps.theme.get());
    }
    // surfaceColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.surfaceColor.isProvided()
          : !newViewProps.surfaceColor.hasSameValue(oldViewProps->surfaceColor)) {
      swiftPart.setSurfaceColor(newViewProps.surfaceColor.get());
    }
    // lineWidth: optional
    if (oldViewProps == nullptr
          ? newViewProps.lineWidth.isProvided()
          : !newViewProps.lineWidth.hasSameValue(oldViewProps->lineWidth)) {
      swiftPart.setLineWidth(newViewProps.lineWidth.get());
    }
    // window: optional
    if (oldViewProps == nullptr
          ? newViewProps.window.isProvided()
          : !newViewProps.window.hasSameValue(oldViewProps->window)) {
      swiftPart.setWindow(newViewProps.window.get());
    }
    // windows: optional
    if (oldViewProps == nullptr
          ? newViewProps.windows.isProvided()
          : !newViewProps.windows.hasSameValue(oldViewProps->windows)) {
      swiftPart.setWindows(newViewProps.windows.get());
    }
    // windowStyle: optional
    if (oldViewProps == nullptr
          ? newViewProps.windowStyle.isProvided()
          : !newViewProps.windowStyle.hasSameValue(oldViewProps->windowStyle)) {
      swiftPart.setWindowStyle(newViewProps.windowStyle.get());
    }
    // onWindowChange: optional
    if (oldViewProps == nullptr
          ? newViewProps.onWindowChange.isProvided()
          : !newViewProps.onWindowChange.hasSameValue(oldViewProps->onWindowChange)) {
      swiftPart.setOnWindowChange(newViewProps.onWindowChange.get());
    }
    // onModeChange: optional
    if (oldViewProps == nullptr
          ? newViewProps.onModeChange.isProvided()
          : !newViewProps.onModeChange.hasSameValue(oldViewProps->onModeChange)) {
      swiftPart.setOnModeChange(newViewProps.onModeChange.get());
    }
    // grid: optional
    if (oldViewProps == nullptr
          ? newViewProps.grid.isProvided()
          : !newViewProps.grid.hasSameValue(oldViewProps->grid)) {
      swiftPart.setGrid(newViewProps.grid.get());
    }
    // badge: optional
    if (oldViewProps == nullptr
          ? newViewProps.badge.isProvided()
          : !newViewProps.badge.hasSameValue(oldViewProps->badge)) {
      swiftPart.setBadge(newViewProps.badge.get());
    }
    // badgeTail: optional
    if (oldViewProps == nullptr
          ? newViewProps.badgeTail.isProvided()
          : !newViewProps.badgeTail.hasSameValue(oldViewProps->badgeTail)) {
      swiftPart.setBadgeTail(newViewProps.badgeTail.get());
    }
    // badgeVariant: optional
    if (oldViewProps == nullptr
          ? newViewProps.badgeVariant.isProvided()
          : !newViewProps.badgeVariant.hasSameValue(oldViewProps->badgeVariant)) {
      swiftPart.setBadgeVariant(newViewProps.badgeVariant.get());
    }
    // momentum: optional
    if (oldViewProps == nullptr
          ? newViewProps.momentum.isProvided()
          : !newViewProps.momentum.hasSameValue(oldViewProps->momentum)) {
      swiftPart.setMomentum(newViewProps.momentum.get());
    }
    // fill: optional
    if (oldViewProps == nullptr
          ? newViewProps.fill.isProvided()
          : !newViewProps.fill.hasSameValue(oldViewProps->fill)) {
      swiftPart.setFill(newViewProps.fill.get());
    }
    // scrub: optional
    if (oldViewProps == nullptr
          ? newViewProps.scrub.isProvided()
          : !newViewProps.scrub.hasSameValue(oldViewProps->scrub)) {
      swiftPart.setScrub(newViewProps.scrub.get());
    }
    // pulse: optional
    if (oldViewProps == nullptr
          ? newViewProps.pulse.isProvided()
          : !newViewProps.pulse.hasSameValue(oldViewProps->pulse)) {
      swiftPart.setPulse(newViewProps.pulse.get());
    }
    // exaggerate: optional
    if (oldViewProps == nullptr
          ? newViewProps.exaggerate.isProvided()
          : !newViewProps.exaggerate.hasSameValue(oldViewProps->exaggerate)) {
      swiftPart.setExaggerate(newViewProps.exaggerate.get());
    }
    // paused: optional
    if (oldViewProps == nullptr
          ? newViewProps.paused.isProvided()
          : !newViewProps.paused.hasSameValue(oldViewProps->paused)) {
      swiftPart.setPaused(newViewProps.paused.get());
    }
    // loading: optional
    if (oldViewProps == nullptr
          ? newViewProps.loading.isProvided()
          : !newViewProps.loading.hasSameValue(oldViewProps->loading)) {
      swiftPart.setLoading(newViewProps.loading.get());
    }
    // emptyText: optional
    if (oldViewProps == nullptr
          ? newViewProps.emptyText.isProvided()
          : !newViewProps.emptyText.hasSameValue(oldViewProps->emptyText)) {
      swiftPart.setEmptyText(newViewProps.emptyText.get());
    }
    // showValue: optional
    if (oldViewProps == nullptr
          ? newViewProps.showValue.isProvided()
          : !newViewProps.showValue.hasSameValue(oldViewProps->showValue)) {
      swiftPart.setShowValue(newViewProps.showValue.get());
    }
    // valueMomentumColor: optional
    if (oldViewProps == nullptr
          ? newViewProps.valueMomentumColor.isProvided()
          : !newViewProps.valueMomentumColor.hasSameValue(oldViewProps->valueMomentumColor)) {
      swiftPart.setValueMomentumColor(newViewProps.valueMomentumColor.get());
    }
    // haptics: optional
    if (oldViewProps == nullptr
          ? newViewProps.haptics.isProvided()
          : !newViewProps.haptics.hasSameValue(oldViewProps->haptics)) {
      swiftPart.setHaptics(newViewProps.haptics.get());
    }
    // degen: optional
    if (oldViewProps == nullptr
          ? newViewProps.degen.isProvided()
          : !newViewProps.degen.hasSameValue(oldViewProps->degen)) {
      swiftPart.setDegen(newViewProps.degen.get());
    }
    // lerpSpeed: optional
    if (oldViewProps == nullptr
          ? newViewProps.lerpSpeed.isProvided()
          : !newViewProps.lerpSpeed.hasSameValue(oldViewProps->lerpSpeed)) {
      swiftPart.setLerpSpeed(newViewProps.lerpSpeed.get());
    }
    // referenceLine: optional
    if (oldViewProps == nullptr
          ? newViewProps.referenceLine.isProvided()
          : !newViewProps.referenceLine.hasSameValue(oldViewProps->referenceLine)) {
      swiftPart.setReferenceLine(newViewProps.referenceLine.get());
    }
    // orderbook: optional
    if (oldViewProps == nullptr
          ? newViewProps.orderbook.isProvided()
          : !newViewProps.orderbook.hasSameValue(oldViewProps->orderbook)) {
      swiftPart.setOrderbook(newViewProps.orderbook.get());
    }
    // valuePrefix: optional
    if (oldViewProps == nullptr
          ? newViewProps.valuePrefix.isProvided()
          : !newViewProps.valuePrefix.hasSameValue(oldViewProps->valuePrefix)) {
      swiftPart.setValuePrefix(newViewProps.valuePrefix.get());
    }
    // valueSuffix: optional
    if (oldViewProps == nullptr
          ? newViewProps.valueSuffix.isProvided()
          : !newViewProps.valueSuffix.hasSameValue(oldViewProps->valueSuffix)) {
      swiftPart.setValueSuffix(newViewProps.valueSuffix.get());
    }
    // valueDecimals: optional
    if (oldViewProps == nullptr
          ? newViewProps.valueDecimals.isProvided()
          : !newViewProps.valueDecimals.hasSameValue(oldViewProps->valueDecimals)) {
      swiftPart.setValueDecimals(newViewProps.valueDecimals.get());
    }
    // currency: optional
    if (oldViewProps == nullptr
          ? newViewProps.currency.isProvided()
          : !newViewProps.currency.hasSameValue(oldViewProps->currency)) {
      swiftPart.setCurrency(newViewProps.currency.get());
    }
    // locale: optional
    if (oldViewProps == nullptr
          ? newViewProps.locale.isProvided()
          : !newViewProps.locale.hasSameValue(oldViewProps->locale)) {
      swiftPart.setLocale(newViewProps.locale.get());
    }
    // useGrouping: optional
    if (oldViewProps == nullptr
          ? newViewProps.useGrouping.isProvided()
          : !newViewProps.useGrouping.hasSameValue(oldViewProps->useGrouping)) {
      swiftPart.setUseGrouping(newViewProps.useGrouping.get());
    }
    // fontFamily: optional
    if (oldViewProps == nullptr
          ? newViewProps.fontFamily.isProvided()
          : !newViewProps.fontFamily.hasSameValue(oldViewProps->fontFamily)) {
      swiftPart.setFontFamily(newViewProps.fontFamily.get());
    }

    // Update hybridRef if it changed
    if (oldViewProps == nullptr
          ? newViewProps.hybridRef.isProvided()
          : !newViewProps.hybridRef.hasSameValue(oldViewProps->hybridRef)) {
      // hybridRef changed - call it with new this
      const auto& maybeFunc = newViewProps.hybridRef.get();
      if (maybeFunc.has_value()) {
        maybeFunc.value()(_hybridView);
      }
    }

    swiftPart.afterUpdate();
  }

  // 3. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return LivelineMobile::LivelineMobileAutolinking::isLivelineRecyclable();
}

- (void)prepareForRecycle {
  [self notifyOnDropView];
  [super prepareForRecycle];
  LivelineMobile::HybridLivelineSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  [self notifyOnDropView];
  [super invalidate];
}
#endif

@end
