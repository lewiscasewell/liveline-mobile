///
/// JHybridLivelineStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridLivelineStateUpdater.hpp"
#include "views/HybridLivelineComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::liveline::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridLivelineState>;

std::shared_ptr<const HybridLivelineProps> JHybridLivelineStateUpdater::getPropsFromStateWrapper(
    jni::alias_ref<JStateWrapper::javaobject> stateWrapper) {
  if (stateWrapper.get() == nullptr) {
    return nullptr;
  }
  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapper.get();
  if (!stateWrapper->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
    throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapperImpl = jni::alias_ref<react::StateWrapperImpl::javaobject>{
    static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)
  };
  std::shared_ptr<const react::State> state = stateWrapperImpl->cthis()->getState();
  if (state == nullptr) {
    return nullptr;
  }
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridLivelineState& data = concreteState->getData();
  const std::shared_ptr<const HybridLivelineProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    throw std::runtime_error("HybridLivelineState's data doesn't contain any props!");
  }
  return props;
}

void JHybridLivelineStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridLivelineSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> newState,
                                           jni::alias_ref<JStateWrapper::javaobject> oldState) {
  std::shared_ptr<JHybridLivelineSpec> hybridView = javaView->getJHybridLivelineSpec();
  std::shared_ptr<const HybridLivelineProps> newProps = getPropsFromStateWrapper(newState);
  std::shared_ptr<const HybridLivelineProps> oldProps = getPropsFromStateWrapper(oldState);
  if (newProps == nullptr) [[unlikely]] {
    throw std::runtime_error("Current StateWrapper doesn't contain any props!");
  }

  // Update only props that differ from the previous State snapshot.
  if (oldProps == nullptr
        ? newProps->data.isProvided()
        : !newProps->data.hasSameValue(oldProps->data)) {
    hybridView->setData(newProps->data.get());
  }
  if (oldProps == nullptr
        ? newProps->series.isProvided()
        : !newProps->series.hasSameValue(oldProps->series)) {
    hybridView->setSeries(newProps->series.get());
  }
  if (oldProps == nullptr
        ? newProps->value.isProvided()
        : !newProps->value.hasSameValue(oldProps->value)) {
    hybridView->setValue(newProps->value.get());
  }
  if (oldProps == nullptr
        ? newProps->mode.isProvided()
        : !newProps->mode.hasSameValue(oldProps->mode)) {
    hybridView->setMode(newProps->mode.get());
  }
  if (oldProps == nullptr
        ? newProps->candles.isProvided()
        : !newProps->candles.hasSameValue(oldProps->candles)) {
    hybridView->setCandles(newProps->candles.get());
  }
  if (oldProps == nullptr
        ? newProps->candleWidth.isProvided()
        : !newProps->candleWidth.hasSameValue(oldProps->candleWidth)) {
    hybridView->setCandleWidth(newProps->candleWidth.get());
  }
  if (oldProps == nullptr
        ? newProps->liveCandle.isProvided()
        : !newProps->liveCandle.hasSameValue(oldProps->liveCandle)) {
    hybridView->setLiveCandle(newProps->liveCandle.get());
  }
  if (oldProps == nullptr
        ? newProps->color.isProvided()
        : !newProps->color.hasSameValue(oldProps->color)) {
    hybridView->setColor(newProps->color.get());
  }
  if (oldProps == nullptr
        ? newProps->theme.isProvided()
        : !newProps->theme.hasSameValue(oldProps->theme)) {
    hybridView->setTheme(newProps->theme.get());
  }
  if (oldProps == nullptr
        ? newProps->surfaceColor.isProvided()
        : !newProps->surfaceColor.hasSameValue(oldProps->surfaceColor)) {
    hybridView->setSurfaceColor(newProps->surfaceColor.get());
  }
  if (oldProps == nullptr
        ? newProps->lineWidth.isProvided()
        : !newProps->lineWidth.hasSameValue(oldProps->lineWidth)) {
    hybridView->setLineWidth(newProps->lineWidth.get());
  }
  if (oldProps == nullptr
        ? newProps->window.isProvided()
        : !newProps->window.hasSameValue(oldProps->window)) {
    hybridView->setWindow(newProps->window.get());
  }
  if (oldProps == nullptr
        ? newProps->windows.isProvided()
        : !newProps->windows.hasSameValue(oldProps->windows)) {
    hybridView->setWindows(newProps->windows.get());
  }
  if (oldProps == nullptr
        ? newProps->windowStyle.isProvided()
        : !newProps->windowStyle.hasSameValue(oldProps->windowStyle)) {
    hybridView->setWindowStyle(newProps->windowStyle.get());
  }
  if (oldProps == nullptr
        ? newProps->onWindowChange.isProvided()
        : !newProps->onWindowChange.hasSameValue(oldProps->onWindowChange)) {
    hybridView->setOnWindowChange(newProps->onWindowChange.get());
  }
  if (oldProps == nullptr
        ? newProps->onModeChange.isProvided()
        : !newProps->onModeChange.hasSameValue(oldProps->onModeChange)) {
    hybridView->setOnModeChange(newProps->onModeChange.get());
  }
  if (oldProps == nullptr
        ? newProps->grid.isProvided()
        : !newProps->grid.hasSameValue(oldProps->grid)) {
    hybridView->setGrid(newProps->grid.get());
  }
  if (oldProps == nullptr
        ? newProps->badge.isProvided()
        : !newProps->badge.hasSameValue(oldProps->badge)) {
    hybridView->setBadge(newProps->badge.get());
  }
  if (oldProps == nullptr
        ? newProps->badgeTail.isProvided()
        : !newProps->badgeTail.hasSameValue(oldProps->badgeTail)) {
    hybridView->setBadgeTail(newProps->badgeTail.get());
  }
  if (oldProps == nullptr
        ? newProps->badgeVariant.isProvided()
        : !newProps->badgeVariant.hasSameValue(oldProps->badgeVariant)) {
    hybridView->setBadgeVariant(newProps->badgeVariant.get());
  }
  if (oldProps == nullptr
        ? newProps->momentum.isProvided()
        : !newProps->momentum.hasSameValue(oldProps->momentum)) {
    hybridView->setMomentum(newProps->momentum.get());
  }
  if (oldProps == nullptr
        ? newProps->fill.isProvided()
        : !newProps->fill.hasSameValue(oldProps->fill)) {
    hybridView->setFill(newProps->fill.get());
  }
  if (oldProps == nullptr
        ? newProps->scrub.isProvided()
        : !newProps->scrub.hasSameValue(oldProps->scrub)) {
    hybridView->setScrub(newProps->scrub.get());
  }
  if (oldProps == nullptr
        ? newProps->pulse.isProvided()
        : !newProps->pulse.hasSameValue(oldProps->pulse)) {
    hybridView->setPulse(newProps->pulse.get());
  }
  if (oldProps == nullptr
        ? newProps->exaggerate.isProvided()
        : !newProps->exaggerate.hasSameValue(oldProps->exaggerate)) {
    hybridView->setExaggerate(newProps->exaggerate.get());
  }
  if (oldProps == nullptr
        ? newProps->paused.isProvided()
        : !newProps->paused.hasSameValue(oldProps->paused)) {
    hybridView->setPaused(newProps->paused.get());
  }
  if (oldProps == nullptr
        ? newProps->loading.isProvided()
        : !newProps->loading.hasSameValue(oldProps->loading)) {
    hybridView->setLoading(newProps->loading.get());
  }
  if (oldProps == nullptr
        ? newProps->emptyText.isProvided()
        : !newProps->emptyText.hasSameValue(oldProps->emptyText)) {
    hybridView->setEmptyText(newProps->emptyText.get());
  }
  if (oldProps == nullptr
        ? newProps->showValue.isProvided()
        : !newProps->showValue.hasSameValue(oldProps->showValue)) {
    hybridView->setShowValue(newProps->showValue.get());
  }
  if (oldProps == nullptr
        ? newProps->valueMomentumColor.isProvided()
        : !newProps->valueMomentumColor.hasSameValue(oldProps->valueMomentumColor)) {
    hybridView->setValueMomentumColor(newProps->valueMomentumColor.get());
  }
  if (oldProps == nullptr
        ? newProps->haptics.isProvided()
        : !newProps->haptics.hasSameValue(oldProps->haptics)) {
    hybridView->setHaptics(newProps->haptics.get());
  }
  if (oldProps == nullptr
        ? newProps->degen.isProvided()
        : !newProps->degen.hasSameValue(oldProps->degen)) {
    hybridView->setDegen(newProps->degen.get());
  }
  if (oldProps == nullptr
        ? newProps->lerpSpeed.isProvided()
        : !newProps->lerpSpeed.hasSameValue(oldProps->lerpSpeed)) {
    hybridView->setLerpSpeed(newProps->lerpSpeed.get());
  }
  if (oldProps == nullptr
        ? newProps->referenceLine.isProvided()
        : !newProps->referenceLine.hasSameValue(oldProps->referenceLine)) {
    hybridView->setReferenceLine(newProps->referenceLine.get());
  }
  if (oldProps == nullptr
        ? newProps->orderbook.isProvided()
        : !newProps->orderbook.hasSameValue(oldProps->orderbook)) {
    hybridView->setOrderbook(newProps->orderbook.get());
  }
  if (oldProps == nullptr
        ? newProps->valuePrefix.isProvided()
        : !newProps->valuePrefix.hasSameValue(oldProps->valuePrefix)) {
    hybridView->setValuePrefix(newProps->valuePrefix.get());
  }
  if (oldProps == nullptr
        ? newProps->valueSuffix.isProvided()
        : !newProps->valueSuffix.hasSameValue(oldProps->valueSuffix)) {
    hybridView->setValueSuffix(newProps->valueSuffix.get());
  }
  if (oldProps == nullptr
        ? newProps->valueDecimals.isProvided()
        : !newProps->valueDecimals.hasSameValue(oldProps->valueDecimals)) {
    hybridView->setValueDecimals(newProps->valueDecimals.get());
  }
  if (oldProps == nullptr
        ? newProps->currency.isProvided()
        : !newProps->currency.hasSameValue(oldProps->currency)) {
    hybridView->setCurrency(newProps->currency.get());
  }
  if (oldProps == nullptr
        ? newProps->locale.isProvided()
        : !newProps->locale.hasSameValue(oldProps->locale)) {
    hybridView->setLocale(newProps->locale.get());
  }
  if (oldProps == nullptr
        ? newProps->useGrouping.isProvided()
        : !newProps->useGrouping.hasSameValue(oldProps->useGrouping)) {
    hybridView->setUseGrouping(newProps->useGrouping.get());
  }
  if (oldProps == nullptr
        ? newProps->fontFamily.isProvided()
        : !newProps->fontFamily.hasSameValue(oldProps->fontFamily)) {
    hybridView->setFontFamily(newProps->fontFamily.get());
  }

  // Update hybridRef if it changed
  if (oldProps == nullptr
        ? newProps->hybridRef.isProvided()
        : !newProps->hybridRef.hasSameValue(oldProps->hybridRef)) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newProps->hybridRef.get();
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
  }
}

} // namespace margelo::nitro::liveline::views
